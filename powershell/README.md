# powershell

Scripts que uso a diario. Algunos requieren correr la consola como admin, esta indicado en cada uno.

Todos devuelven objetos, no texto. Eso significa que podes pipearlos a `Format-Table`, `Export-Csv` o `Out-GridView` sin parseo manual.
